export const REF = "cyrilla-mist/nexus-ai";
export const CAPTURED_AT = "2026-08-07T12:00:00Z";
export const SHA = { branch: "1111111111111111111111111111111111111111", commitA: "2222222222222222222222222222222222222222", commitB: "3333333333333333333333333333333333333333", pr: "4444444444444444444444444444444444444444", tag: "5555555555555555555555555555555555555555" };
export const createRequestedLimits = (overrides = {}) => ({ commits: 2, issues: 1, pullRequests: 1, releases: 1, tags: 1, ...overrides });
const clone = value => Array.isArray(value) ? value.map(clone) : value && typeof value === "object" ? Object.fromEntries(Object.entries(value).map(([k, v]) => [k, clone(v)])) : value;
export const createRepositoryResponse = (overrides = {}) => ({ name: "nexus-ai", fullName: REF, defaultBranch: "main", archived: false, visibility: "public", updatedAt: "2026-08-07T11:00:00Z", ...overrides });
export const createBranchResponse = (overrides = {}) => ({ name: "main", headSha: SHA.branch, ...overrides });
export const createCommit = (overrides = {}) => ({ sha: SHA.commitA, authoredAt: "2026-08-06T09:00:00Z", committedAt: "2026-08-06T10:00:00Z", message: "Define source snapshot boundary\nprivate body", authorEmail: "private@example.test", body: "private", ...overrides });
export const createIssue = (overrides = {}) => ({ number: 17, title: "Review source boundary", state: "open", createdAt: "2026-08-03T12:00:00Z", updatedAt: "2026-08-04T12:00:00Z", closedAt: null, body: "private", comments: [{ body: "private" }], ...overrides });
export const createPullRequest = (overrides = {}) => ({ number: 23, title: "Document source snapshot contract", state: "closed", draft: false, merged: true, createdAt: "2026-08-02T12:00:00Z", updatedAt: "2026-08-04T14:00:00Z", closedAt: "2026-08-04T14:00:00Z", mergedAt: "2026-08-04T14:00:00Z", headSha: SHA.pr, baseRef: "main", body: "private", comments: [], reviews: [], ...overrides });
export const createRelease = (overrides = {}) => ({ immutableId: "release-7", tagName: "v0.1.0", name: "Snapshot contract design fixture", draft: false, prerelease: false, createdAt: "2026-08-01T10:00:00Z", publishedAt: "2026-08-01T12:00:00Z", ...overrides });
export const createTag = (overrides = {}) => ({ name: "v0.1.0", targetSha: SHA.tag, ...overrides });
export const createReadResult = (items, overrides = {}) => ({ items, pagesRead: 1, continuationAvailable: false, ...overrides });
export function createGitHubClientFixture(overrides = {}) { const calls = []; const data = { repository: createRepositoryResponse(), branch: createBranchResponse(), commits: [createCommit(), createCommit({ sha: SHA.commitB, authoredAt: "2026-08-05T09:00:00Z", committedAt: "2026-08-05T10:00:00Z", message: "Preserve source authority boundaries" })], issues: [createIssue()], pullRequests: [createPullRequest()], releases: [createRelease()], tags: [createTag()], ...overrides }; const client = { calls, async getRepository(input) { calls.push(["repository", input]); return clone(data.repository); }, async getDefaultBranch(input) { calls.push(["branch", input]); return clone(data.branch); }, ...Object.fromEntries(["Commits", "Issues", "PullRequests", "Releases", "Tags"].map(name => { const key = name[0].toLowerCase() + name.slice(1); return [`list${name}`, async input => { calls.push([key, input]); return createReadResult(clone(data[key])); }]; })) }; return client; }
export const cloneFixture = clone;
export const createThrowingClient = (error = new Error("fixture unavailable")) => { const client = createGitHubClientFixture(); for (const method of Object.keys(client).filter(key => key.startsWith("get") || key.startsWith("list"))) client[method] = async () => { throw error; }; return client; };
