{
  "name": "nexus-ai",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Nexus AI Project Atlas multi-turn product experience.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "check": "node --check worker/index.js && node --check core/nexus-core.js && node --check atlas/project-atlas/index.js && node --check atlas/project-atlas/stage.js && node --check model/deepseek-client.js && node --check model/model-router.js && node --check frontend/session-state.js && node --check frontend/app.js",
    "verify:multiturn": "node tests/verify-multiturn.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
