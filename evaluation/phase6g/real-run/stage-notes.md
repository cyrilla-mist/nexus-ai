# Phase 6G real-run stages

- `pre-action`: freeze and upload the human-confirmed checkpoint, fresh pre-action evidence, `VALID` assessment, Re-entry Package, and expected postcondition while `main` still equals the confirmed cursor.
- `armed`: the pre-action artifact run ID has been recorded. The Phase 6G PR itself may then be merged as the genuine bounded external repository action. The merge-triggered `main` workflow must independently re-read GitHub, verify the outcome, durably close the checkpoint, and prove a subsequent re-entry starts from checkpoint v2.
